{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["src/**", "tsconfig.build.json"],
			"outputs": ["dist/**"]
		},
		"test": {
			"dependsOn": ["^build"],
			"inputs": ["**/*"]
		}
	}
}
